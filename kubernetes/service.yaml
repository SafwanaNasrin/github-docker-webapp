apiVersion: v1
kind: Service
metadata:
  name: github-docker-webapp-service
spec:
  type: NodePort
  selector:
    app: github-docker-webapp
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
